# KangWH.github.io

Next.js + React + Tailwind CSS + TypeScript로 만든 개인 사이트입니다.

```bash
npm install
npm run dev
```

[http://localhost:3000](http://localhost:3000)에서 확인할 수 있습니다. 정적 사이트는 `npm run build`로 `out/`에 생성됩니다.
